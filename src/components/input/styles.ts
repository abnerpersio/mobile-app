import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderColor: '#888',
    borderWidth: 1,
    borderRadius: 8,
    height: 48,
    paddingHorizontal: 16,
    color: '#333',
  },
  inputFocused: {
    borderColor: '#222',
  },
});
