import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textarea: {
    borderColor: '#888',
    borderWidth: 1,
    borderRadius: 8,
    padding: 16,
    minHeight: 72,
    maxHeight: 144,
    color: '#333',
  },
  textareaFocused: {
    borderColor: '#222',
  },
});
